CM.make "$cml/cml.cm";
open CML;
val chan1: int chan = channel();
val chan2: int chan = channel();
fun milkyway ch ch1 n= case recv(ch1) of 
write => send(ch,n)

(*wrap (recvEvt ch1,mailbox ch ch1)*)
TextIO.print(Int.toString(x)^"\n"),
